{
  "name": "project-roadmap",
  "version": "1.0.0",
  "description": "Project Roadmap API using Notion",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.14"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
